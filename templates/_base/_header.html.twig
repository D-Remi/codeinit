<style>
    /* side bar */

    .spanNav{
        font-size:30px;
        cursor: pointer;
        color: #fff;
        font-family:'Lato',sans-serif;
        float: right;
        padding: 20px
    }

    .sidenav {
        height: 100vh;
        width: 0;
        position: fixed;
        z-index: 1;
        top: 0;
        right: 0;
        background-color: rgba(0,0,0,0.45);
        overflow-x: hidden;
        transition: 0.5s;
        padding-top: 60px;
        text-align:center;
        display:flex;
        flex-wrap: wrap;
        justify-content: center;
    }

    .sidenav div{
        width:20%;
        margin: 0 20px;
    }

    .sidenav .closebtn {
        position: absolute;
        top: 0;
        right: 25px;
        font-size: 36px;
        margin-left: 50px;
    }
</style>
<header class="container-fluid p-0 m-0">
        <div class="row py-2 m-0 justify-content-between align-items-center px-1" style="background: linear-gradient(180deg,#67A8FF,#66C7FF,#65E9FF)">
            <div class="col-lg-2 xSmall-6 col-md-3 col-3">
                <img src="{{ asset('build/images/LogoCodeInit2.png') }}" class="img-fluid">
            </div>
            <span onclick="openSlide()" class="spanNav col-lg-1 col-md-1 col-2 small-4 xSmall-2 medium-2"><i class="fa fa-bars fa-1x"></i></span>
            <div id="mySidenav" class="sidenav">
                <a href="javascript:void(0)" class="closebtn" onclick="closeSlide()">&times;</a>
                <ul>
                    <li><a href="{{ path('homepage') }}" class="text-white">Accueil</a></li>
                    <li><a href="{{ path('app_cart') }}" class="text-white">Mon Panier</a></li>
                    {% if app.user %}
                        <li><a href="{{ path('profile') }}" class="text-white">Mon Profil</a></li>
                        {% if is_granted('ROLE_ADMIN') %}
                            <li><a href="{{ path('app_admin') }}" class="text-white">Panel Admin</a></li>
                        {% endif %}
                        <li><a href="{{ path('app_logout') }}" class="text-white">Déconnexion</a></li>
                    {% else %}
                        <li><a href="{{ path('app_register') }}" class="text-white">Inscription</a></li>
                        <li><a href="{{ path('app_login') }}" class="text-white">Connexion</a></li>
                    {% endif %}
                </ul>
            </div>
        </div>
</header>
<script src="{{ asset('js/sideNav.js') }}"></script>

